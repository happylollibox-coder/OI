# Archived dashboard JSON (Cube-backed)

These files were produced by `refresh_data.py` and are now superseded by Cube.
The dashboard uses Cube when `VITE_CUBE_API_URL` is set; these files are kept for reference.

`refresh_data.py` continues to write here when run; the dashboard does not read them.
